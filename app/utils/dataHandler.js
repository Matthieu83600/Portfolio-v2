// Utilisation de require pour charger les données JSON
const jsonData = require('../constants/projects.json');

// Exportez les données pour les utiliser ailleurs dans votre application
module.exports = {
  jsonData
};
